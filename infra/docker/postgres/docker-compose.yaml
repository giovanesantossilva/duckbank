services:
  postgres:
    image: postgres:18
    container_name: postgres
    restart: always
    tty: true
    volumes:
      - postgres:/data/postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
volumes:
  postgres:
    driver: local
